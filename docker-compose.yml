services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: cwh
      POSTGRES_PASSWORD: cwh
      POSTGRES_DB: cloud_waste_hunter
    ports:
      - "5433:5432"
    volumes:
      - cwh-postgres-data:/var/lib/postgresql/data

volumes:
  cwh-postgres-data:
